# Runtime databases (regenerated; the app under test uses LibraryTestFramework/seed_db.py)
Library/prisma/dev.db
Library/server.log

# Local browser/driver downloads and CI run artifacts
/tmp/
/home/
/e2e-reports.zip

# Thesis build workspace (generated PDFs, crops, node_modules); the final
# documents it produces live in docs/
/thesis_build/

# Test-run artifact: rewritten on every pytest run; CI generates its own
/LibraryTestFramework/coverage.json

# MS Office lock/temp files (~$...docx while a document is open in Word)
~$*
